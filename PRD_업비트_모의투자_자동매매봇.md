# PRD — 업비트 모의투자(페이퍼 트레이딩) 자동매매 봇

> **문서 목적**: 이 문서는 Claude Code가 바로 구현에 착수할 수 있도록 작성된 제품 요구사항 정의서(PRD)다.
> **버전**: v0.1 (초안 — 사용자와 티키타카로 다듬는 중)
> **작성일**: 2026-06-04
> **소유자**: LeanUp (admin@leanup.kr)

---

## 0. 한 줄 요약

업비트의 **실시간 시세는 진짜로** 받아오되, **주문은 가상 잔고에서만 체결**시키는 모의투자 자동매매 봇. 실제 돈을 한 푼도 넣지 않고 전략을 검증·운영하는 것이 목표다.

> ⚠️ **중요 전제**: 업비트는 증권사 같은 공식 "모의투자 계좌"를 제공하지 않는다. 따라서 모의투자는 **봇 내부에 가상 지갑(Paper Wallet)을 직접 구현**하여 실현한다. 실제 주문 API(`buy_market_order`, `sell_market_order` 등)는 이 단계에서 **절대 호출하지 않는다.**

---

## 1. 배경 & 목표

### 1.1 배경
- 사용자는 자동매매로 수익을 내고 싶지만, 실제 자금 투입 전에 전략을 안전하게 검증하고 싶다.
- 코인(업비트)을 선택한 이유: API 접근이 쉽고, 24시간 거래되며, 계좌 심사 없이 시세 데이터를 즉시 받아올 수 있다.

### 1.2 목표 (이번 버전 범위)
1. 업비트 실시간 시세를 안정적으로 수집한다.
2. 가상 잔고(KRW + 코인 보유량)를 관리하는 페이퍼 트레이딩 엔진을 만든다.
3. 최소 1개 이상의 매매 전략을 플러그인 형태로 끼워 자동매매 루프를 돌린다.
4. 모든 가상 거래와 손익(P&L)을 기록하고, 성과를 리포트로 확인한다.

### 1.3 비목표 (이번 버전에서 안 하는 것)
- 실제 주문/실거래 (다음 버전)
- 선물/마진/레버리지
- 머신러닝 기반 예측 모델
- 모바일 앱 / 화려한 GUI (CLI + 간단한 HTML 리포트로 충분)

### 1.4 성공 기준
- 봇이 24시간 죽지 않고 시세를 받아 전략 신호에 따라 가상 매매를 수행한다.
- 모든 거래가 누락 없이 기록되고, 언제든 현재 가상 자산/수익률을 조회할 수 있다.
- 동일 기간 과거 데이터에 대해 **백테스트 결과와 실시간 페이퍼 트레이딩 결과가 논리적으로 일치**한다(체결/수수료 로직 동일).

---

## 2. 사용자 & 사용 시나리오

**주 사용자**: 봇 운영자 본인(개발자 1인).

**핵심 시나리오**
1. 설정 파일에서 거래 대상 코인, 전략, 파라미터, 시작 가상자본을 정한다.
2. `python run.py --mode paper` 로 봇을 켠다.
3. 봇이 주기적으로 시세를 받아 전략을 평가하고, 신호가 나면 가상 매수/매도를 체결한다.
4. 사용자는 언제든 `python report.py` 또는 HTML 대시보드로 현재 수익률·거래내역·보유자산을 본다.
5. 마음에 들면 파라미터를 바꿔가며 반복 검증한다.

---

## 3. 기능 요구사항

### 3.1 시세 수집 모듈 (Market Data)
- **소스**: 업비트 공개(Quotation) API — 인증 불필요. `pyupbit` 라이브러리 사용 권장.
- **수집 항목**:
  - 현재가: `pyupbit.get_current_price(ticker)`
  - 캔들(OHLCV): `pyupbit.get_ohlcv(ticker, interval, count)` — 전략 지표 계산용
  - (선택) 호가/체결: 실시간 정밀도가 필요하면 WebSocket(`pyupbit.WebSocketManager`) 사용
- **대상 마켓**: KRW 마켓 (예: `KRW-BTC`, `KRW-ETH`). 여러 종목 동시 지원 가능하게 설계.
- **폴링 주기**: 설정값(`poll_interval_sec`, 기본 10초). 전략 타임프레임에 맞게 조정.
- **레이트 리밋 준수**: 주문 외 요청은 초당 30회 / 분당 900회 한도. 요청 사이 적절한 sleep과 백오프 재시도(429/네트워크 오류 시) 구현.
- **장애 대응**: 일시적 네트워크 오류 시 재시도, 연속 실패 시 로그 경고 후 다음 주기로 스킵(봇이 죽지 않게).

### 3.2 페이퍼 트레이딩 엔진 (Paper Wallet) — ⭐ 핵심
실제 주문 대신 내부 가상 장부로 체결을 시뮬레이션한다.
- **가상 잔고 상태**:
  - `cash_krw`: 보유 원화(가상)
  - `positions`: { ticker: { quantity, avg_buy_price } }
- **주문 시뮬레이션**:
  - 시장가 매수: 현재가 × 수량으로 체결, 수수료 차감 후 포지션·현금 갱신
  - 시장가 매도: 현재가로 청산, 수수료 차감 후 현금 증가
  - (선택) 지정가 주문: 다음 틱 가격이 지정가 조건을 만족하면 체결
- **수수료 모델**: KRW 마켓 기본 수수료 **0.05%** (`fee_rate` 설정값으로 조정 가능). 매수·매도 양쪽 모두 적용.
- **슬리피지 모델(선택, 현실성↑)**: 체결가에 소량 슬리피지(`slippage_bp`)를 더해 낙관적 백테스트를 방지.
- **무결성 규칙**: 현금이 부족하면 매수 거부, 보유량이 부족하면 매도 거부(주문 거절 로그 남김).
- **최소 주문 금액**: 업비트 KRW 마켓 최소 주문 5,000원 규칙을 시뮬레이션에도 반영.

### 3.3 전략 모듈 (Strategy) — 플러그인 구조
공통 인터페이스를 정의하고, 전략을 갈아끼울 수 있게 한다.

```python
class Strategy(Protocol):
    def on_data(self, ticker: str, candles: DataFrame, price: float) -> Signal:
        ...  # 반환: BUY / SELL / HOLD (+ 수량/비중 정보)
```

**기본 제공 전략(최소 2개 구현)**:
1. **이동평균 교차 (MA Crossover)** — 단기 MA가 장기 MA를 상향 돌파 시 매수, 하향 돌파 시 매도. (파라미터: `short`, `long`)
2. **변동성 돌파 (Larry Williams)** — 당일 시가 + (전일 고저 변동폭 × k) 돌파 시 매수, 다음 날 시가 청산. 한국 코인 자동매매 커뮤니티의 대표 입문 전략. (파라미터: `k`, 기본 0.5)
3. (선택) **RSI 역추세** — RSI 30 이하 매수, 70 이상 매도.

전략은 파라미터를 설정 파일에서 주입받는다. 전략 추가가 쉽도록 `strategies/` 디렉터리에 파일 단위로 분리.

### 3.4 리스크 관리 (Risk Management)
- **포지션 사이징**: 1회 매수 비중(`position_size_pct`, 예: 가용 현금의 20%) 또는 고정 금액.
- **손절(Stop-Loss)**: 평단 대비 `stop_loss_pct` 하락 시 강제 매도.
- **익절(Take-Profit, 선택)**: `take_profit_pct` 도달 시 매도.
- **일일 최대 손실 한도**: 하루 손실이 `max_daily_loss_pct` 초과 시 당일 신규 매수 중단.
- **종목당 최대 보유 비중 제한**.

### 3.5 실행 루프 & 스케줄러 (Orchestrator)
- 메인 루프: `시세 수집 → 전략 평가 → 리스크 체크 → (가상)주문 → 기록` 을 `poll_interval_sec` 주기로 반복.
- 멀티 종목: 설정된 모든 티커에 대해 순회 처리.
- 우아한 종료: Ctrl+C(SIGINT) 시 현재 상태 저장 후 종료.
- 상태 영속화: 봇 재시작 시 직전 가상 잔고/포지션을 복원(상태를 DB나 JSON에 저장).

### 3.6 데이터 기록 & 리포트 (Logging & Reporting)
- **저장소**: SQLite (간단·파일 1개). 테이블 예시:
  - `trades`: 시각, 티커, 매수/매도, 가격, 수량, 수수료, 체결 후 현금/포지션
  - `equity_curve`: 시각, 총 평가자산(현금 + 코인 평가액)
  - `signals`(선택): 전략 신호 로그
- **리포트 항목**:
  - 누적 수익률 / 실현·미실현 손익
  - 총 거래 횟수, 승률, 평균 손익비
  - MDD(최대 낙폭), (선택) 샤프 비율
  - 자산 곡선(equity curve) 차트
- **출력 형태**: CLI 요약 + 단일 HTML 리포트(차트 포함). 추후 실시간 대시보드로 확장 가능.

### 3.7 설정 & 안전장치 (Config & Safety)
- **설정 파일**: `config.yaml` (또는 `.env` + yaml). 코드 수정 없이 파라미터 변경.
- **모드 스위치**: `mode: paper | live`. **기본값은 반드시 `paper`.** `live`는 이번 버전에서 미구현이며, 호출 시 명시적 에러로 막아 실거래 사고를 원천 차단.
- **API 키 분리**: 시세 조회는 키가 필요 없음. (향후 실거래용) 키는 `.env`에만 두고 절대 코드/깃에 커밋 금지. `.gitignore`에 `.env`, `*.db` 포함.

---

## 4. 기술 스택 & 아키텍처

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Python 3.11+ | 코인 자동매매 생태계 표준 |
| 업비트 연동 | `pyupbit` | 시세 조회는 인증 불필요 |
| 데이터 처리 | `pandas` | 지표 계산·캔들 처리 |
| 저장소 | SQLite (`sqlite3`) | 파일 1개, 의존성 최소 |
| 설정 | `pyyaml` + `python-dotenv` | |
| 차트/리포트 | `matplotlib` 또는 단일 HTML + Chart.js | |
| 스케줄 | 자체 `while` 루프 + `time.sleep` | 외부 스케줄러 불필요 |
| 테스트 | `pytest` | 체결/수수료/손절 로직 단위 테스트 필수 |

**디렉터리 구조(제안)**
```
upbit-paper-bot/
├── config.yaml
├── .env.example
├── run.py                 # 메인 실행 (실시간 페이퍼 루프)
├── backtest.py            # 과거 데이터 백테스트
├── report.py              # 리포트 생성
├── core/
│   ├── market_data.py     # 시세 수집
│   ├── paper_wallet.py    # 가상 잔고/체결 엔진 ⭐
│   ├── orchestrator.py    # 메인 루프
│   ├── risk.py            # 리스크 관리
│   └── storage.py         # SQLite 기록
├── strategies/
│   ├── base.py            # Strategy 인터페이스
│   ├── ma_crossover.py
│   └── volatility_breakout.py
└── tests/
    └── test_paper_wallet.py ...
```

---

## 5. 백테스트 요구사항 (강력 권장 선행 단계)

실시간 페이퍼 트레이딩 전에 **과거 데이터로 전략을 먼저 검증**한다.
- `pyupbit.get_ohlcv`로 과거 캔들을 받아 전략을 시뮬레이션.
- **페이퍼 트레이딩 엔진과 동일한 체결/수수료 로직을 재사용**(코드 공유)하여 백테스트와 실시간 결과의 일관성 보장.
- 출력: 위 3.6의 리포트 항목 동일.
- 주의: 과최적화(overfitting) 경계. 인샘플/아웃샘플 분리 권장.

---

## 6. 단계별 구현 로드맵 (마일스톤)

- **M1 — 시세 수집**: 업비트에서 현재가·캔들 안정적으로 받아 콘솔 출력.
- **M2 — 페이퍼 월렛**: 가상 잔고/매수/매도/수수료/무결성 + 단위 테스트.
- **M3 — 전략 1개 + 백테스트**: MA 교차 또는 변동성 돌파로 백테스트 리포트 생성.
- **M4 — 실시간 페이퍼 루프**: 오케스트레이터로 24시간 가상 자동매매 + SQLite 기록.
- **M5 — 리스크 관리 & 리포트**: 손절/포지션 사이징 + HTML 성과 리포트.
- **M6 — 안정화**: 장애 복구, 상태 영속화, 로깅 정리.

---

## 7. 리스크 & 주의사항 (정직하게)

- **자동매매로 꾸준히 수익 내는 건 매우 어렵다.** 백테스트가 좋아도 실시간/실거래에서 무너지는 경우가 흔하다(과최적화, 슬리피지, 시장 변화).
- 이 봇은 **수익을 보장하지 않으며, 투자 권유가 아니다.** 의사결정은 사용자 본인 책임.
- 페이퍼 단계에서 충분히 검증되기 전까지 **실거래 모드는 잠가둔다.**
- 실거래로 넘어갈 때는 별도의 보안·키관리·예외처리 강화가 필요하다(다음 버전에서 별도 PRD).

---

## 8. 다음 버전 후보 (Backlog)
- 실거래(live) 모드 + 안전장치(주문 한도, 이중 확인)
- 실시간 웹 대시보드(자산/포지션/로그 라이브)
- 알림(텔레그램/슬랙)으로 체결·손절 통지
- 다중 전략 포트폴리오 / 자산 배분
- 파라미터 자동 최적화(그리드 서치) + 워크포워드 검증

---

## 9. 열린 질문 (사용자와 결정 필요)

1. **거래 대상 코인**: BTC만? 아니면 ETH·여러 알트도?
2. **전략 타임프레임**: 분봉 단타? 일봉 스윙? (변동성 돌파는 보통 일봉)
3. **시작 가상자본**: 얼마로 잡을까? (예: 1,000만원 가상)
4. **첫 전략**: MA 교차 vs 변동성 돌파 — 뭐부터?
5. **리포트 형태**: CLI 요약이면 충분? HTML 차트까지?

*위 질문에 답해주면 PRD를 확정하고, 바로 Claude Code에 넘길 수 있게 다듬을게.*
